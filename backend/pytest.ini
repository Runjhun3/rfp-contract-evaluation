[pytest]
pythonpath = .
testpaths = tests/unit
markers =
    golden: runs against real AWS (Bedrock, Textract); run on demand only
    integration: full API flow on an empty local Postgres; run with `pytest tests/integration`
